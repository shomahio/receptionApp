# require 'rails_helper'

# RSpec.describe "Informations", type: :request do

#   before do
#     @information = FactoryBot.create(:information)
#   end

#   describe "GET /informations" do
    
#     it 'indexアクションにリクエストすると正常にレスポンスが返ってくる' do 
#       get root_path
#       expect(response.status).to eq 200
#     end

#     it 'indexアクションにリクエストするとレスポンスにワード検索欄が存在する' do 
#       get root_path
#       expect(response.body).to include(@tweet.text)
#     end

#     it 'indexアクションにリクエストすると正常にレスポンスが返ってくる' do 
#       get root_path
#       expect(response.status).to eq 200
#     end

#     it 'indexアクションにリクエストすると正常にレスポンスが返ってくる' do 
#       get root_path
#       expect(response.status).to eq 200
#     end

#   end
# end
